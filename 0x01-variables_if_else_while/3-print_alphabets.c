#include <stdio.h>

/**
 * main - Entry point
 * lets play what I call the alphabet game again
 * but  this time uppercase and lowercase
 * Return: 0 (SUCCESS)
 */

int main(void)
{
char low;
char app;

low = 'a';
app = 'A';
while (low <= 'z')
{
putchar(low);
low++;
}
while (b <= 'Z')
{
putchar(app);
app++;
}
putchar('\n');
return (0);
}
